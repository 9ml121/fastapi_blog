# 登录页面 UI 设计文档

> **文档版本**：v1.0  
> **创建日期**：2024-12-10  
> **参考设计**：[Crelay 登录页面](https://crelay.net/#/login)  
> **状态**：待评审

---

## 1. 设计概述

本文档基于 Crelay 登录页面进行对标分析，结合项目现有的前后端 API 功能，设计博客系统的登录页面 UI。

### 1.1 参考页面分析
![[登录页面ui.png]]

**Crelay 登录页面特点**：

| 特性         | 描述                                   |
| ------------ | -------------------------------------- |
| **整体布局** | 页面居中的卡片式设计，简洁大气         |
| **背景设计** | 浅色渐变背景，营造干净专业感           |
| **主色调**   | 蓝色系（#3B82F6 类似），用于按钮和链接 |
| **表单设计** | 精致的输入框，带有图标前缀             |
| **辅助功能** | 记住我、忘记密码链接、注册跳转         |
| **顶部导航** | 主题切换（暗/亮）、语言切换            |

---

## 2. 我们的登录页面设计

### 2.1 网站品牌设计

**网站名称**：**InkFlow**（墨流）

> 设计理念：InkFlow 寓意文字如墨水般流畅自如，象征博客内容的自由表达和思想的流动。

**Logo 设计方案**：

```
设计元素：
- 简化的钢笔笔尖图标 + 流动曲线
- 配合现代无衬线字体 "InkFlow"
- 主色：深蓝色 (#1E40AF) 或渐变蓝 (#3B82F6 → #1E40AF)
```

**Logo SVG 示意**（使用 Lucide 图标组合）：

```vue
<!-- 使用 lucide-vue-next 图标组合 -->
<template>
  <div class="logo">
    <PenLine class="logo-icon" />
    <span class="logo-text">InkFlow</span>
  </div>
</template>
```

### 2.2 整体布局

```
┌────────────────────────────────────────────────────────────┐
│  [🌙/☀️ 主题] ────────────────────────────── [语言切换] │  顶部工具栏
├────────────────────────────────────────────────────────────┤
│                                                            │
│                                                            │
│              ┌──────────────────────────┐                  │
│              │     🖊️  InkFlow          │  Logo 区域      │
│              │     ───────────────────  │                  │
│              │                          │                  │
│              │  📧  用户名 / 邮箱       │  输入框          │
│              │  ┌──────────────────────┐│                  │
│              │  │                      ││                  │
│              │  └──────────────────────┘│                  │
│              │                          │                  │
│              │  🔒  密码      [👁️]     │  密码框          │
│              │  ┌──────────────────────┐│                  │
│              │  │                      ││                  │
│              │  └──────────────────────┘│                  │
│              │                          │                  │
│              │  ☑️ 记住我   忘记密码？  │  辅助功能        │
│              │                          │                  │
│              │  ┌──────────────────────┐│                  │
│              │  │       登 录          ││  主按钮          │
│              │  └──────────────────────┘│                  │
│              │                          │                  │
│              │  没有账号？ 立即注册     │  注册跳转        │
│              └──────────────────────────┘                  │
│                                                            │
│                                                            │
├────────────────────────────────────────────────────────────┤
│              © 2024 InkFlow. All rights reserved.          │  页脚
└────────────────────────────────────────────────────────────┘
```

---

## 3. UI 设计要求

### 3.1 配色方案

```css
/* 颜色变量定义 */
:root {
  /* 主色调 - 蓝色系 */
  --color-primary: #3b82f6; /* 主蓝色 */
  --color-primary-hover: #2563eb; /* 悬停态 */
  --color-primary-light: #eff6ff; /* 浅蓝背景 */

  /* 中性色 - 灰色系 */
  --color-text-primary: #1f2937; /* 主文字 */
  --color-text-secondary: #6b7280; /* 次级文字 */
  --color-text-placeholder: #9ca3af; /* 占位符 */

  /* 背景色 */
  --color-bg-page: #f3f4f6; /* 页面背景 */
  --color-bg-card: #ffffff; /* 卡片背景 */
  --color-border: #e5e7eb; /* 边框色 */

  /* 功能色 */
  --color-success: #10b981; /* 成功绿 */
  --color-error: #ef4444; /* 错误红 */
  --color-warning: #f59e0b; /* 警告黄 */
}

/* 暗色主题 */
[data-theme='dark'] {
  --color-primary: #60a5fa;
  --color-primary-hover: #3b82f6;
  --color-primary-light: #1e3a5f;

  --color-text-primary: #f9fafb;
  --color-text-secondary: #9ca3af;
  --color-text-placeholder: #6b7280;

  --color-bg-page: #111827;
  --color-bg-card: #1f2937;
  --color-border: #374151;
}
```

### 3.2 字体规范

```css
/* 字体栈 */
--font-family-base: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;

/* 字号规范 */
--font-size-xs: 0.75rem; /* 12px - 辅助文字 */
--font-size-sm: 0.875rem; /* 14px - 表单标签 */
--font-size-base: 1rem; /* 16px - 正文 */
--font-size-lg: 1.125rem; /* 18px - 按钮文字 */
--font-size-xl: 1.5rem; /* 24px - 页面标题 */
--font-size-2xl: 2rem; /* 32px - Logo */
```

### 3.3 间距规范

```css
/* 间距变量 */
--spacing-xs: 0.25rem; /* 4px */
--spacing-sm: 0.5rem; /* 8px */
--spacing-md: 1rem; /* 16px */
--spacing-lg: 1.5rem; /* 24px */
--spacing-xl: 2rem; /* 32px */
--spacing-2xl: 3rem; /* 48px */
```

### 3.4 组件尺寸

| 组件         | 属性   | 值                            |
| ------------ | ------ | ----------------------------- |
| **登录卡片** | 宽度   | 400px (max-width: 90vw)       |
|              | 内边距 | 32px～48px                    |
|              | 圆角   | 16px                          |
|              | 阴影   | 0 4px 24px rgba(0,0,0,0.08)   |
| **输入框**   | 高度   | 48px                          |
|              | 圆角   | 8px                           |
|              | 边框   | 1px solid var(--color-border) |
| **按钮**     | 高度   | 48px                          |
|              | 圆角   | 8px                           |
|              | 字号   | 16px                          |
| **图标**     | 尺寸   | 20px                          |
|              | 颜色   | var(--color-text-secondary)   |

---

## 4. 交互设计要求

### 4.1 输入框交互

| 状态     | 样式变化                           |
| -------- | ---------------------------------- |
| **默认** | 灰色边框，浅灰背景图标             |
| **聚焦** | 蓝色边框 + 蓝色图标 + 浅蓝色外发光 |
| **错误** | 红色边框 + 红色图标 + 下方错误提示 |
| **禁用** | 灰色背景，文字变淡                 |

```css
/* 输入框聚焦效果 */
.input:focus {
  border-color: var(--color-primary);
  box-shadow: 0 0 0 3px var(--color-primary-light);
  outline: none;
}

/* 输入框错误状态 */
.input.error {
  border-color: var(--color-error);
}
```

### 4.2 按钮交互

| 状态       | 样式变化                   |
| ---------- | -------------------------- |
| **默认**   | 蓝色背景，白色文字         |
| **悬停**   | 加深背景色，轻微上移       |
| **点击**   | 进一步加深，轻微下沉       |
| **加载中** | 显示旋转加载图标，禁用点击 |
| **禁用**   | 灰色背景，降低透明度       |

```css
/* 按钮基础样式 */
.btn-primary {
  background: var(--color-primary);
  color: white;
  transition: all 0.2s ease;
}

/* 悬停效果 */
.btn-primary:hover:not(:disabled) {
  background: var(--color-primary-hover);
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(59, 130, 246, 0.3);
}

/* 点击效果 */
.btn-primary:active:not(:disabled) {
  transform: translateY(0);
}

/* 加载状态 */
.btn-primary.loading {
  pointer-events: none;
  opacity: 0.8;
}
```

### 4.3 密码可见性切换

```vue
<!-- 密码输入框带眼睛图标 -->
<div class="input-wrapper">
  <Lock class="input-icon" />
  <input 
    :type="showPassword ? 'text' : 'password'" 
    v-model="password"
    placeholder="请输入密码"
  />
  <button @click="showPassword = !showPassword" class="toggle-btn">
    <Eye v-if="showPassword" />
    <EyeOff v-else />
  </button>
</div>
```

### 4.4 表单验证反馈

- **实时验证**：输入框失焦时触发验证
- **错误提示**：输入框下方显示红色错误信息
- **摇动动画**：错误时输入框轻微左右摇动

```css
/* 错误摇动动画 */
@keyframes shake {
  0%,
  100% {
    transform: translateX(0);
  }
  25% {
    transform: translateX(-5px);
  }
  75% {
    transform: translateX(5px);
  }
}

.input.error {
  animation: shake 0.3s ease-in-out;
}
```

---

## 5. 技术实现要点

### 5.1 使用的图标库

项目使用 **lucide-vue-next** 图标库，登录页面涉及的图标：

```typescript
// 需要导入的图标
import {
  PenLine, // Logo 图标
  Mail, // 邮箱/用户名图标
  Lock, // 密码图标
  Eye, // 显示密码
  EyeOff, // 隐藏密码
  Sun, // 亮色主题
  Moon, // 暗色主题
  Loader2, // 加载中动画
  AlertCircle, // 错误提示
  CheckCircle, // 成功提示
} from 'lucide-vue-next'
```

### 5.2 组件结构

```
src/
├── views/
│   └── LoginView.vue          # 登录页面（布局容器）
│
└── modules/auth/
    ├── components/
    │   ├── LoginForm.vue      # 登录表单（核心组件）
    │   ├── LoginCard.vue      # 登录卡片容器
    │   └── BrandLogo.vue      # 品牌 Logo 组件
    │
    ├── composables/
    │   └── useLoginForm.ts    # 登录表单逻辑
    │
    └── styles/
        └── login.css          # 登录页面样式
```

### 5.3 已有 API 对接

项目已有的认证 API：

| 功能 | 端点                    | 方法 | Content-Type                      |
| ---- | ----------------------- | ---- | --------------------------------- |
| 登录 | `/api/v1/auth/login`    | POST | application/x-www-form-urlencoded |
| 注册 | `/api/v1/auth/register` | POST | application/json                  |

**登录请求示例**：

```typescript
// 已有代码 - modules/auth/api.ts
export async function loginApi(params: LoginParams): Promise<LoginResponse> {
  const formData = new URLSearchParams()
  formData.append('username', params.username)
  formData.append('password', params.password)

  const response = await api.post('/auth/login', formData, {
    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
  })
  return response.data
}
```

### 5.4 表单验证逻辑

```typescript
// composables/useLoginForm.ts
interface LoginFormState {
  username: string
  password: string
  rememberMe: boolean
}

interface FormErrors {
  username: string
  password: string
}

// 验证规则
const validateRules = {
  username: (value: string) => {
    if (!value) return '请输入用户名或邮箱'
    return ''
  },
  password: (value: string) => {
    if (!value) return '请输入密码'
    if (value.length < 6) return '密码至少6位'
    return ''
  },
}
```

### 5.5 响应式适配

```css
/* 移动端适配 */
@media (max-width: 480px) {
  .login-card {
    width: 100%;
    margin: 16px;
    padding: 24px 20px;
    border-radius: 12px;
  }

  .logo-text {
    font-size: 1.5rem;
  }
}

/* 平板适配 */
@media (min-width: 481px) and (max-width: 768px) {
  .login-card {
    width: 90%;
    max-width: 400px;
  }
}
```

---

## 6. 关键样式代码

### 6.1 登录页面容器

```css
/* 登录页面布局 */
.login-page {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  background: var(--color-bg-page);
}

/* 主内容区 - 垂直居中 */
.login-main {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: var(--spacing-xl);
}
```

### 6.2 登录卡片

```css
/* 登录卡片样式 */
.login-card {
  width: 100%;
  max-width: 400px;
  background: var(--color-bg-card);
  border-radius: 16px;
  padding: var(--spacing-2xl);
  box-shadow: 0 4px 24px rgba(0, 0, 0, 0.08);
}

/* 暗色主题阴影调整 */
[data-theme='dark'] .login-card {
  box-shadow: 0 4px 24px rgba(0, 0, 0, 0.4);
  border: 1px solid var(--color-border);
}
```

### 6.3 输入框组

```css
/* 输入框容器 */
.input-group {
  position: relative;
  margin-bottom: var(--spacing-lg);
}

/* 输入框样式 */
.input-field {
  width: 100%;
  height: 48px;
  padding: 0 var(--spacing-md);
  padding-left: 44px; /* 为图标留空间 */
  border: 1px solid var(--color-border);
  border-radius: 8px;
  font-size: var(--font-size-base);
  background: var(--color-bg-card);
  color: var(--color-text-primary);
  transition:
    border-color 0.2s,
    box-shadow 0.2s;
}

/* 左侧图标 */
.input-icon {
  position: absolute;
  left: 14px;
  top: 50%;
  transform: translateY(-50%);
  width: 20px;
  height: 20px;
  color: var(--color-text-secondary);
  pointer-events: none;
}

/* 聚焦时图标变色 */
.input-field:focus ~ .input-icon {
  color: var(--color-primary);
}
```

---

## 7. 实现优先级

| 优先级 | 功能         | 说明                        |
| ------ | ------------ | --------------------------- |
| P0     | 基础登录表单 | 用户名、密码输入 + 登录按钮 |
| P0     | 表单验证     | 空值检查、错误提示          |
| P0     | API 对接     | 调用已有登录接口            |
| P1     | 密码可见切换 | 眼睛图标切换密码显示        |
| P1     | 响应式布局   | 移动端适配                  |
| P2     | 记住我功能   | 持久化登录状态              |
| P2     | 主题切换     | 亮/暗主题支持               |
| P3     | 忘记密码链接 | 后续扩展                    |
| P3     | 加载动画     | 登录过程中的反馈            |

---

## 8. 设计效果预览

### 8.1 亮色主题效果

```
┌──────────────────────────────────┐
│                                  │
│         🖊️ InkFlow              │
│                                  │
│   ┌────────────────────────────┐ │
│   │ 📧  johndoe@example.com   │ │
│   └────────────────────────────┘ │
│                                  │
│   ┌────────────────────────────┐ │
│   │ 🔒  ••••••••     👁️      │ │
│   └────────────────────────────┘ │
│                                  │
│   ☑️ 记住我        忘记密码？   │
│                                  │
│   ┌────────────────────────────┐ │
│   │          登 录             │ │ ← 蓝色背景
│   └────────────────────────────┘ │
│                                  │
│      没有账号？ 立即注册         │
│                                  │
└──────────────────────────────────┘
```

---

## 9. 相关文档

- [登录功能概要设计](./登录功能概设.md)
- [技术选型和设计理念](../技术选型和设计理念.md)
