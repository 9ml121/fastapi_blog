# 前端开发规范

> 本文档定义了项目前端开发的统一规范，包括文件命名、代码风格、目录结构等，确保团队协作的一致性和代码的可维护性。

---

## 📚 目录

1. [文件命名规范](#文件命名规范)
2. [目录结构规范](#目录结构规范)
3. [代码风格规范](#代码风格规范)
4. [导入语句规范](#导入语句规范)
5. [组件开发规范](#组件开发规范)

---

## 文件命名规范

### 核心原则

- **一致性优先**：全项目统一命名风格
- **语义化命名**：文件名要能清晰表达文件用途
- **区分文件类型**：通过命名规范快速识别文件类型

---

### 命名规则总览

| 文件类型 | 命名规范 | 示例 | 说明 |
|---------|---------|------|------|
| **Vue 组件** | PascalCase | `UserCard.vue`<br>`FormInput.vue` | 每个单词首字母大写 |
| **页面组件** | PascalCase + View 后缀 | `LoginView.vue`<br>`RegisterView.vue` | 区分页面和普通组件 |
| **Composables** | camelCase + use 前缀 | `useAuth.ts`<br>`useMarkdown.ts` | 符合 Vue 3 Composition API 习惯 |
| **Utils（工具函数）** | camelCase | `validators.ts`<br>`token.ts`<br>`errorHandler.ts` | 小驼峰命名 |
| **Stores（状态管理）** | camelCase + .store 后缀 | `auth.store.ts`<br>`toast.store.ts` | 便于识别 Pinia Store |
| **API 文件** | camelCase + .api 后缀 | `auth.api.ts`<br>`user.api.ts` | 便于识别 API 层 |
| **类型定义** | camelCase + .types 后缀 | `editor.types.ts`<br>`user.types.ts` | 便于识别类型文件 |
| **测试文件** | camelCase + .spec/.test 后缀 | `useSelection.spec.ts`<br>`auth.api.test.ts` | 与源文件同名 + 测试后缀 |
| **CSS 文件** | camelCase | `main.css`<br>`theme.css` | 与 TS 文件保持一致 |

---

### 详细说明

#### 1. Vue 组件（PascalCase）

**规则**：每个单词首字母大写

```
✅ 正确：
components/
  ├── BrandLogo.vue
  ├── FormInput.vue
  └── ToastContainer.vue

❌ 错误：
components/
  ├── brandLogo.vue          # 首字母应大写
  ├── form-input.vue         # 不用短横线
  └── toast_container.vue    # 不用下划线
```

**原因**：
- 视觉区分：一眼看出是 Vue 组件
- 符合惯例：Vue 社区主流做法
- 导入清晰：`import UserCard from '@/components/UserCard.vue'`

---

#### 2. 页面组件（PascalCase + View 后缀）

**规则**：PascalCase + `View` 后缀

```
✅ 正确：
views/
  ├── LoginView.vue
  ├── RegisterView.vue
  └── BaseLayout.vue

❌ 错误：
views/
  ├── Login.vue              # 缺少 View 后缀
  ├── register-view.vue      # 不用短横线
  └── UserProfile.vue        # 应该是 UserProfileView.vue
```

**原因**：
- 区分页面和组件：`LoginView` vs `LoginForm`
- 路由对应清晰：路由 `/login` 对应 `LoginView.vue`

---

#### 3. Composables（camelCase + use 前缀）

**规则**：小驼峰 + `use` 前缀

```
✅ 正确：
composables/
  ├── useAuth.ts
  ├── useMarkdown.ts
  └── useSelection.ts

❌ 错误：
composables/
  ├── Auth.ts                # 缺少 use 前缀
  ├── use-markdown.ts        # 不用短横线
  └── UseSelection.ts        # 不用 PascalCase
```

**原因**：
- 符合 Vue 3 习惯：官方推荐 `useXxx` 命名
- 一眼识别：知道是 Composition API
- 自动提示：编辑器输入 `use` 自动补全

---

#### 4. Utils（camelCase）

**规则**：小驼峰命名

```
✅ 正确：
utils/
  ├── validators.ts
  ├── token.ts
  └── errorHandler.ts

❌ 错误：
utils/
  ├── Validators.ts          # 不用 PascalCase
  ├── error-handler.ts       # 不用短横线
  └── api_client.ts          # 不用下划线
```

**原因**：
- 统一风格：与 composables、stores 保持一致
- 导入简洁：`import { getToken } from '@/utils/token'`

---

#### 5. Stores（camelCase + .store 后缀）

**规则**：小驼峰 + `.store` 后缀

```
✅ 正确：
stores/
  ├── auth.store.ts
  ├── toast.store.ts
  └── user.store.ts

❌ 错误：
stores/
  ├── authStore.ts           # 使用后缀而非驼峰
  ├── Auth.store.ts          # 不用 PascalCase
  └── toast-store.ts         # 不用短横线
```

**原因**：
- 快速识别：一眼看出是 Pinia Store
- 命名空间清晰：`auth.store.ts` 对应 `useAuthStore()`

---

#### 6. API 文件（camelCase + .api 后缀）

**规则**：小驼峰 + `.api` 后缀

```
✅ 正确：
api/
  ├── auth.api.ts
  ├── user.api.ts
  └── post.api.ts

❌ 错误：
api/
  ├── authApi.ts             # 使用后缀而非驼峰
  ├── Auth.api.ts            # 不用 PascalCase
  └── user-api.ts            # 不用短横线
```

**原因**：
- 快速识别：一眼看出是 API 层
- 模块分组：`auth.api.ts` 包含所有认证相关 API

---

#### 7. 类型定义（camelCase + .types 后缀）

**规则**：小驼峰 + `.types` 后缀

```
✅ 正确：
types/
  ├── editor.types.ts
  ├── user.types.ts
  └── api.types.ts

❌ 错误：
types/
  ├── editorTypes.ts         # 使用后缀而非驼峰
  ├── Editor.types.ts        # 不用 PascalCase
  └── user-types.ts          # 不用短横线
```

**原因**：
- 快速识别：一眼看出是类型定义文件
- 避免混淆：区分逻辑代码和类型定义

---

#### 8. 测试文件（camelCase + .spec/.test 后缀）

**规则**：与源文件同名 + `.spec.ts` 或 `.test.ts`

```
✅ 正确：
__tests__/
  ├── useSelection.spec.ts       # 对应 useSelection.ts
  └── auth.api.test.ts           # 对应 auth.api.ts

❌ 错误：
__tests__/
  ├── useSelectionSpec.ts        # 使用后缀而非驼峰
  ├── UseSelection.spec.ts       # 不用 PascalCase
  └── auth-api.test.ts           # 不用短横线
```

**原因**：
- 对应清晰：测试文件名与源文件完全对应
- 工具支持：测试框架自动识别 `.spec.ts` 和 `.test.ts`

---

### 特殊说明

#### index.ts 的使用

**规则**：目录入口文件统一使用 `index.ts`

```
✅ 正确：
api/
  ├── index.ts              # API 入口
  ├── auth.api.ts
  └── user.api.ts

modules/editor/
  ├── index.ts              # 模块入口
  ├── components/
  └── composables/

❌ 错误：
api/
  └── api.ts                # 应该用 index.ts

modules/editor/
  └── editor.ts             # 应该用 index.ts
```

**原因**：
- 简化导入：`import api from '@/api'` 而非 `@/api/index`
- 符合惯例：Node.js 和前端框架的标准做法

---

## 目录结构规范

### 标准目录结构

```
frontend/src/
├── api/                    # API 层
│   ├── index.ts            # Axios 实例配置
│   ├── auth.api.ts         # 认证相关 API
│   └── user.api.ts         # 用户相关 API
│
├── assets/                 # 静态资源
│   ├── images/             # 图片
│   ├── fonts/              # 字体
│   └── icons/              # 图标
│
├── components/             # 全局组件
│   ├── BrandLogo.vue
│   ├── FormInput.vue
│   └── ToastContainer.vue
│
├── composables/            # 全局 Composables（可选）
│   └── useGlobalConfig.ts
│
├── modules/                # 功能模块（按业务分组）
│   ├── editor/             # 编辑器模块
│   │   ├── components/     # 模块组件
│   │   ├── composables/    # 模块 Composables
│   │   ├── utils/          # 模块工具函数
│   │   ├── types/          # 模块类型定义（可选）
│   │   └── index.ts        # 模块入口
│   │
│   └── auth/               # 认证模块（开发中）
│       └── index.ts
│
├── router/                 # 路由配置
│   └── index.ts
│
├── stores/                 # Pinia 状态管理
│   ├── auth.store.ts
│   └── toast.store.ts
│
├── styles/                 # 全局样式
│   ├── main.css            # 主样式
│   └── variables.css       # CSS 变量
│
├── utils/                  # 全局工具函数
│   ├── errorHandler.ts     # 错误处理
│   ├── validators.ts       # 表单验证
│   └── token.ts            # Token 管理
│
├── views/                  # 页面组件（路由页面）
│   ├── BaseLayout.vue
│   ├── LoginView.vue
│   └── RegisterView.vue
│
├── App.vue                 # 根组件
└── main.ts                 # 应用入口
```

---

### 模块化组织原则

#### 按功能分组（推荐）

```
✅ 推荐（按功能）：
modules/
  ├── editor/               # 编辑器功能
  │   ├── components/
  │   ├── composables/
  │   └── utils/
  │
  └── auth/                 # 认证功能
      ├── components/
      ├── composables/
      └── utils/

❌ 不推荐（按类型）：
src/
  ├── components/
  │   ├── EditorToolbar.vue
  │   ├── LoginForm.vue
  │   └── RegisterForm.vue  # 不同功能混在一起
  │
  └── composables/
      ├── useMarkdown.ts
      ├── useAuth.ts
      └── useLogin.ts       # 不同功能混在一起
```

**原因**：
- 高内聚：相关代码放在一起
- 易维护：删除功能时只需删除对应目录
- 清晰明了：功能边界清晰

---

## 代码风格规范

### TypeScript 类型定义

#### 接口命名

```typescript
// ✅ 正确：接口名用 PascalCase，不加 I 前缀
export interface User {
  id: string
  username: string
  email: string
}

export interface AuthResponse {
  access_token: string
  user: User
}

// ❌ 错误：不要用 I 前缀（这是 C# 风格）
export interface IUser {  // ❌ 不推荐
  id: string
}
```

---

#### 类型别名命名

```typescript
// ✅ 正确：类型别名用 PascalCase
export type ToastType = 'success' | 'error' | 'warning' | 'info'

export type UserId = string

// ❌ 错误
export type toastType = 'success' | 'error'  // ❌ 首字母应大写
```

---

### 函数命名

#### Composables

```typescript
// ✅ 正确：use 前缀 + 小驼峰
export function useAuth() { ... }
export function useMarkdown() { ... }

// ❌ 错误
export function getAuth() { ... }      // ❌ 应该用 use 前缀
export function UseMarkdown() { ... }  // ❌ 不用 PascalCase
```

---

#### 工具函数

```typescript
// ✅ 正确：动词开头，小驼峰
export function getToken(): string | null { ... }
export function validateEmail(email: string): string | null { ... }
export function formatDate(date: Date): string { ... }

// ❌ 错误
export function Token(): string | null { ... }        // ❌ 缺少动词
export function validate_email(email: string) { ... } // ❌ 不用下划线
```

---

### 常量命名

```typescript
// ✅ 正确：全大写 + 下划线
export const TOKEN_KEY = 'auth_token'
export const API_BASE_URL = '/api/v1'
export const MAX_FILE_SIZE = 5 * 1024 * 1024  // 5MB

// ❌ 错误
export const tokenKey = 'auth_token'  // ❌ 应该全大写
export const ApiBaseUrl = '/api/v1'  // ❌ 应该全大写
```

---

## 导入语句规范

### 导入顺序

```typescript
// 1. Vue 核心库
import { ref, computed, onMounted } from 'vue'
import { useRouter } from 'vue-router'

// 2. 第三方库
import axios from 'axios'
import { marked } from 'marked'

// 3. 项目内部模块（按层级）
import { useAuthStore } from '@/stores/auth.store'
import { loginApi } from '@/api/auth.api'
import { getToken } from '@/utils/token'
import FormInput from '@/components/FormInput.vue'

// 4. 类型导入（单独分组）
import type { User, AuthResponse } from '@/api/auth.api'
import type { ToastType } from '@/stores/toast.store'
```

---

### 导入别名

```typescript
// ✅ 推荐：使用 @ 别名
import { useAuthStore } from '@/stores/auth.store'
import FormInput from '@/components/FormInput.vue'

// ❌ 不推荐：使用相对路径
import { useAuthStore } from '../../../stores/auth.store'
import FormInput from '../../components/FormInput.vue'
```

**配置路径别名**（`vite.config.ts`）：

```typescript
export default defineViteConfig({
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url))
    }
  }
})
```

---

## 组件开发规范

### Vue 组件结构顺序

```vue
<script setup lang="ts">
// 1. 导入语句
import { ref, computed } from 'vue'
import { useRouter } from 'vue-router'

// 2. 类型定义（组件内部）
interface Props {
  title: string
  count?: number
}

// 3. Props 和 Emits
const props = defineProps<Props>()
const emit = defineEmits<{
  submit: [value: string]
}>()

// 4. 状态（响应式数据）
const isLoading = ref(false)
const formData = ref({ ... })

// 5. 计算属性
const isValid = computed(() => ...)

// 6. 方法
const handleSubmit = () => { ... }

// 7. 生命周期钩子
onMounted(() => { ... })
</script>

<template>
  <!-- 模板内容 -->
</template>

<style scoped>
/* 组件样式 */
</style>
```

---

### 组件命名（模板中）

```vue
<!-- ✅ 正确：PascalCase -->
<template>
  <FormInput v-model="email" />
  <UserCard :user="currentUser" />
</template>

<!-- ❌ 错误：kebab-case -->
<template>
  <form-input v-model="email" />    <!-- ❌ 应该用 PascalCase -->
  <user-card :user="currentUser" /> <!-- ❌ 应该用 PascalCase -->
</template>
```

**原因**：
- 与导入名称一致：`import FormInput` → `<FormInput />`
- 区分原生元素：`<FormInput />` vs `<input />`

---

## 工具配置

### ESLint 配置

项目使用 ESLint 自动检查代码风格：

```bash
# 检查代码
pnpm lint

# 自动修复
pnpm lint --fix
```

---

### Prettier 配置

项目使用 Prettier 自动格式化代码：

```bash
# 格式化代码
pnpm format
```

**配置文件**：`.prettierrc.json`

```json
{
  "semi": false,
  "singleQuote": true,
  "printWidth": 100,
  "trailingComma": "all"
}
```

---

## 快速参考

### 文件命名速查表

| 文件类型 | 命名示例 | 错误示例 |
|---------|---------|---------|
| 组件 | `UserCard.vue` | `userCard.vue`, `user-card.vue` |
| 页面 | `LoginView.vue` | `Login.vue`, `login-view.vue` |
| Composable | `useAuth.ts` | `Auth.ts`, `use-auth.ts` |
| Utils | `validators.ts` | `Validators.ts`, `validate-utils.ts` |
| Store | `auth.store.ts` | `authStore.ts`, `auth-store.ts` |
| API | `user.api.ts` | `userApi.ts`, `user-api.ts` |
| Types | `user.types.ts` | `userTypes.ts`, `user-types.ts` |
| Test | `useAuth.spec.ts` | `useAuthSpec.ts`, `use-auth.spec.ts` |

---

### 实际项目示例

```
src/
├── components/
│   ├── BrandLogo.vue          ✅ PascalCase
│   └── FormInput.vue          ✅ PascalCase
│
├── views/
│   ├── LoginView.vue          ✅ PascalCase + View
│   └── RegisterView.vue       ✅ PascalCase + View
│
├── composables/
│   ├── useAuth.ts             ✅ camelCase + use
│   └── useMarkdown.ts         ✅ camelCase + use
│
├── utils/
│   ├── validators.ts          ✅ camelCase
│   ├── token.ts               ✅ camelCase
│   └── errorHandler.ts        ✅ camelCase
│
├── stores/
│   ├── auth.store.ts          ✅ camelCase + .store
│   └── toast.store.ts         ✅ camelCase + .store
│
└── api/
    ├── auth.api.ts            ✅ camelCase + .api
    └── user.api.ts            ✅ camelCase + .api
```

---

## 规范执行

### 1. 代码审查（Code Review）

- 新文件必须符合命名规范
- Pull Request 时检查文件命名

### 2. 自动化工具

- ESLint：检查代码风格
- Prettier：自动格式化
- Git Hooks：提交前自动检查（可选）

### 3. 持续改进

- 定期更新规范文档
- 团队讨论优化方案
- 记录特殊情况和例外

---

**最后更新**：2025-12-20  
**维护者**：fastapi_blog 开发团队
