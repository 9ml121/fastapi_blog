# 前端新手入门指南

> 本指南专为前端新手设计，重点讲解网页布局、CSS 组件化等核心知识。

---

## 目录

1. [HTML/CSS/JS 三剑客](#1-htmlcssjs-三剑客)
2. [CSS 盒模型](#2-css-盒模型)
3. [Flexbox 布局](#3-flexbox-布局)
4. [CSS Grid 布局](#4-css-grid-布局)
5. [CSS 组件化](#5-css-组件化)
6. [响应式设计](#6-响应式设计)
7. [CSS 变量](#7-css-变量)
8. [学习资源](#8-学习资源)

---

## 1. HTML/CSS/JS 三剑客

| 技术 | 职责 | 类比 |
|------|------|------|
| **HTML** | 结构（骨架） | 房子的框架 |
| **CSS** | 样式（外观） | 装修和家具 |
| **JavaScript** | 行为（交互） | 电器和开关 |

```html
<!-- HTML：结构 -->
<button class="btn">点击我</button>
```

```css
/* CSS：样式 */
.btn {
  background: blue;
  color: white;
}
```

```javascript
// JavaScript：行为
document.querySelector('.btn').onclick = () => alert('Hello!')
```

---

## 2. CSS 盒模型

每个 HTML 元素都是一个"盒子"，由四层组成：

```
┌─────────────────────────────────┐
│            margin               │  ← 外边距（与其他元素的距离）
│  ┌─────────────────────────┐    │
│  │        border           │    │  ← 边框
│  │  ┌─────────────────┐    │    │
│  │  │    padding      │    │    │  ← 内边距（内容与边框的距离）
│  │  │  ┌─────────┐    │    │    │
│  │  │  │ content │    │    │    │  ← 内容
│  │  │  └─────────┘    │    │    │
│  │  └─────────────────┘    │    │
│  └─────────────────────────┘    │
└─────────────────────────────────┘
```

### 关键属性

```css
.box {
  width: 200px;         /* 内容宽度 */
  padding: 20px;        /* 内边距 */
  border: 1px solid #ccc; /* 边框 */
  margin: 10px;         /* 外边距 */
  
  /* 推荐：让 width 包含 padding 和 border */
  box-sizing: border-box;
}
```

### box-sizing 区别

| 值 | width 包含 |
|----|------------|
| `content-box`（默认） | 仅内容 |
| `border-box`（推荐） | 内容 + padding + border |

---

## 3. Flexbox 布局

**Flexbox** 是一维布局系统，解决**水平/垂直排列**问题。

### 核心概念

```
┌──────────────────────────────────┐
│  flex-container（容器）           │
│  ┌─────┐ ┌─────┐ ┌─────┐         │
│  │item │ │item │ │item │         │
│  └─────┘ └─────┘ └─────┘         │
│  ← 主轴 (main axis) →             │
└──────────────────────────────────┘
         ↑ 交叉轴 (cross axis)
```

### 容器属性

```css
.container {
  display: flex;                    /* 开启 Flexbox */
  flex-direction: row;              /* 主轴方向：row(横)|column(竖) */
  justify-content: center;          /* 主轴对齐：center|space-between|flex-start */
  align-items: center;              /* 交叉轴对齐：center|flex-start|stretch */
  gap: 16px;                        /* 子元素间距 */
  flex-wrap: wrap;                  /* 换行：nowrap|wrap */
}
```

### 子元素属性

```css
.item {
  flex: 1;                          /* 占据剩余空间 */
  flex-grow: 1;                     /* 放大比例 */
  flex-shrink: 0;                   /* 缩小比例（0=不缩小） */
  flex-basis: 200px;                /* 初始大小 */
}
```

### 常见布局示例

```css
/* 水平垂直居中 */
.center {
  display: flex;
  justify-content: center;
  align-items: center;
}

/* 两端对齐 */
.space-between {
  display: flex;
  justify-content: space-between;
}

/* 垂直排列 */
.column {
  display: flex;
  flex-direction: column;
}
```

---

## 4. CSS Grid 布局

**Grid** 是二维布局系统，适合**复杂网格布局**。

### 核心概念

```
┌─────────┬─────────┬─────────┐
│  1      │  2      │  3      │  ← 行 (row)
├─────────┼─────────┼─────────┤
│  4      │  5      │  6      │
├─────────┼─────────┼─────────┤
│  7      │  8      │  9      │
└─────────┴─────────┴─────────┘
     ↑         ↑         ↑
    列 (column)
```

### 基础用法

```css
.grid-container {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;  /* 3 列等宽 */
  grid-template-rows: 100px 100px;     /* 2 行固定高度 */
  gap: 16px;                           /* 行列间距 */
}
```

### 常用单位

| 单位 | 含义 |
|------|------|
| `fr` | 剩余空间的比例 |
| `px` | 固定像素 |
| `auto` | 自适应内容 |
| `minmax(min, max)` | 最小/最大值 |

### 示例：经典布局

```css
/* 侧边栏 + 主内容 */
.layout {
  display: grid;
  grid-template-columns: 250px 1fr;  /* 侧边栏固定宽度，主内容自适应 */
  height: 100vh;
}
```

---

## 5. CSS 组件化

### 5.1 BEM 命名规范

**BEM** = Block（块）+ Element（元素）+ Modifier（修饰符）

```css
/* Block：独立组件 */
.card { }

/* Element：组件内部元素，用 __ 连接 */
.card__title { }
.card__body { }

/* Modifier：状态变体，用 -- 连接 */
.card--featured { }
.btn--primary { }
.btn--disabled { }
```

### 5.2 多类名组合模式

```html
<button class="btn btn-primary btn-lg btn-block">
  提交
</button>
```

```css
/* 基础类 */
.btn {
  padding: 12px 24px;
  border-radius: 8px;
  cursor: pointer;
}

/* 变体类 */
.btn-primary { background: #3B82F6; color: white; }
.btn-secondary { background: #6B7280; color: white; }

/* 尺寸类 */
.btn-sm { padding: 8px 16px; font-size: 14px; }
.btn-lg { padding: 16px 32px; font-size: 18px; }

/* 布局类 */
.btn-block { width: 100%; }
```

### 5.3 Vue 组件化

```vue
<!-- BaseButton.vue -->
<template>
  <button :class="['btn', `btn-${variant}`, { 'btn-block': block }]">
    <slot />
  </button>
</template>

<script setup>
defineProps({
  variant: { type: String, default: 'primary' },
  block: { type: Boolean, default: false }
})
</script>
```

---

## 6. 响应式设计

### 媒体查询

```css
/* 移动端优先 */
.container {
  padding: 16px;
}

/* 平板 */
@media (min-width: 768px) {
  .container {
    padding: 24px;
  }
}

/* 桌面 */
@media (min-width: 1024px) {
  .container {
    padding: 32px;
    max-width: 1200px;
    margin: 0 auto;
  }
}
```

### 常用断点

| 设备 | 宽度 |
|------|------|
| 手机 | < 768px |
| 平板 | 768px - 1023px |
| 桌面 | ≥ 1024px |
| 大屏 | ≥ 1440px |

---

## 7. CSS 变量

### 定义和使用

```css
:root {
  /* 定义变量 */
  --color-primary: #3B82F6;
  --color-text: #1F2937;
  --spacing-md: 16px;
  --radius-md: 8px;
}

.btn {
  /* 使用变量 */
  background: var(--color-primary);
  padding: var(--spacing-md);
  border-radius: var(--radius-md);
}
```

### 主题切换

```css
/* 亮色主题（默认） */
:root {
  --bg-color: #FFFFFF;
  --text-color: #1F2937;
}

/* 暗色主题 */
[data-theme="dark"] {
  --bg-color: #1A1A2E;
  --text-color: #E5E7EB;
}
```

---

## 8. 学习资源

### 🎮 交互式练习（强烈推荐）

|资源|学什么|难度|
|---|---|---|
|[Flexbox Froggy](https://flexboxfroggy.com/)|Flexbox 布局|⭐ 入门|
|[Grid Garden](https://cssgridgarden.com/)|CSS Grid 布局|⭐ 入门|
|[Flexbox Defense](http://www.flexboxdefense.com/)|Flexbox 塔防游戏|⭐⭐|
|[CSS Diner](https://flukeout.github.io/)|CSS 选择器|⭐ 入门|

### 🎨 UI 组件库源码（学习参考）

| 项目                 | 特点              | 链接                                                                                   |
| ------------------ | --------------- | ------------------------------------------------------------------------------------ |
| **Naive UI**       | Vue 3，中文文档，代码清晰 | [github.com/tusen-ai/naive-ui](https://github.com/tusen-ai/naive-ui)                 |
| **Element Plus**   | Vue 3，企业级组件库    | [github.com/element-plus/element-plus](https://github.com/element-plus/element-plus) |
| **Ant Design Vue** | Vue 3，组件丰富      | [antdv.com](https://antdv.com/)                                                      |
### 📖 权威文档

- [MDN Web Docs](https://developer.mozilla.org/zh-CN/) - 最权威的前端文档
- [CSS-Tricks](https://css-tricks.com/) - CSS 技巧和教程（英文）

### 📚 系统教程

- [Flexbox 完全指南](https://css-tricks.com/snippets/css/a-guide-to-flexbox/)
- [Grid 完全指南](https://css-tricks.com/snippets/css/complete-guide-grid/)

### 🛠️ 实践建议

1. **仿写真实网站**：用开发者工具（F12）研究你喜欢的网站
2. **从小组件开始**：按钮 → 卡片 → 表单 → 页面布局
3. **多写多练**：CSS 需要大量练习才能熟练

### 📝 实战练习项目

|项目|练习内容|推荐|
|---|---|---|
|**仿写登录页**|布局、表单、按钮|⭐⭐⭐|
|**仿写卡片列表**|Flexbox、Grid|⭐⭐⭐|
|**仿写导航栏**|定位、响应式|⭐⭐|
|**仿写仪表盘**|综合布局|⭐⭐⭐|

---

> 💡 **记住**：CSS 布局的核心是 **Flexbox + Grid**，掌握这两个就能应对 90% 的布局需求！
