# 阶段4：处理用户输入（防抖记录）实现指南

## 一、任务目标

让用户输入的文字也能被记录到历史，实现完整的撤销/重做功能。

---

## 二、当前问题

### 测试一下

在编辑器中输入 "hello"，然后按 Ctrl+Z：

**现象**：❌ 什么都不会发生

**原因**：用户输入不会触发 `pushTransaction()`

```typescript
// EditorContent.vue
const handleInput = () => {
  if (editorRef.value) {
    const newContent = editorRef.value.innerHTML
    emit('update:modelValue', newContent)
    editorState.content = newContent
    // ⚠️ 没有调用 pushTransaction！
  }
}
```

---

## 三、核心挑战：防抖合并

### 问题：如果每次输入都记录会怎样？

```typescript
// ❌ 错误做法
const handleInput = () => {
  // ...
  historyAPI.pushTransaction(newContent, '输入文本')
}

// 用户输入 "hello"
输入 'h' → 记录1个 transaction
输入 'e' → 记录1个 transaction
输入 'l' → 记录1个 transaction
输入  'l' → 记录1个 transaction
输入 'o' → 记录1个 transaction

// 结果：5个 transactions！

// 用户按 Ctrl+Z
撤销1次 → "hell"
撤销2次 → "hel"
撤销3次 → "he"
撤销4次 → "h"
撤销5次 → ""

// ❌ 用户体验很差！用户期望撤销1次就删除整个 "hello"
```

### 解决方案：防抖（Debounce）

**核心思想**：等用户停止输入一段时间（如500ms）后，再记录一次。

```typescript
用户输入 'h' → 启动定时器（500ms）
用户输入 'e' → 重置定时器（500ms）
用户输入 'l' → 重置定时器（500ms）
用户输入 'l' → 重置定时器（500ms）
用户输入 'o' → 重置定时器（500ms）
用户停止输入
  → 等待 500ms
  → 定时器到期
  → 记录1个 transaction ✅

结果：只有1个 transaction！

用户按 Ctrl+Z
撤销1次 → "" ✅ 符合预期
```

---

## 四、实现步骤

### 步骤1: 定义防抖定时器 ✅

**位置**：`EditorContent.vue` 的 `<script setup>` 顶部

**新增代码**：

```typescript
// =========== 响应式数据 ================
const editorRef = ref<HTMLDivElement | null>(null)
const editorState = reactive<EditorState>({ ... })

// ⚡️ 新增：防抖定时器
let inputTimer: number | null = null
```

**知识点**：

- 使用 `let` 而不是 `const`，因为需要重新赋值
- 类型是 `number | null`（定时器ID或null）

---

### 步骤2: 修改 handleInput 实现防抖 🔥

**位置**：`EditorContent.vue` 中的 `handleInput` 函数

**现有代码**：

```typescript
const handleInput = () => {
  if (editorRef.value) {
    const newContent = editorRef.value.innerHTML
    emit('update:modelValue', newContent)
    editorState.content = newContent
    editorState.isDirty = true
  }
}
```

**修改后**：

```typescript
const handleInput = () => {
  if (editorRef.value) {
    const newContent = editorRef.value.innerHTML

    // 立即更新显示（不等防抖）
    emit('update:modelValue', newContent)
    editorState.content = newContent
    editorState.isDirty = true

    // ⚡️ 防抖记录历史（500ms 内的输入合并为一个 transaction）
    if (inputTimer !== null) {
      clearTimeout(inputTimer) // 清除之前的定时器
    }

    inputTimer = window.setTimeout(() => {
      historyAPI.pushTransaction(newContent, '输入文本')
      inputTimer = null // 重置定时器
    }, 500)
  }
}
```

**重点理解**：

#### 为什么要 `clearTimeout`？

```typescript
用户输入 'h'
  → inputTimer = setTimeout(...)  // 设置定时器A（500ms后执行）

用户输入 'e'（200ms后）
  → clearTimeout(inputTimer)      // ⚠️ 取消定时器A
  → inputTimer = setTimeout(...)  // 设置新的定时器B（500ms后执行）

用户输入 'l'（300ms后）
  → clearTimeout(inputTimer)      // ⚠️ 取消定时器B
  → inputTimer = setTimeout(...)  // 设置新的定时器C（500ms后执行）

...

用户停止输入
  → 等待 500ms
  → 定时器C 到期
  → 执行 pushTransaction ✅
```

#### 为什么要立即更新显示？

```typescript
// ✅ 正确：立即更新
emit('update:modelValue', newContent)
editorState.content = newContent

// 然后才防抖记录
setTimeout(() => {
  historyAPI.pushTransaction(...)
}, 500)

// 如果不立即更新，用户会感觉卡顿：
// 用户输入 'h' → 等 500ms 才看到 'h' ❌
```

#### 为什么是 500ms？

- **太短（如100ms）**：用户快速打字时，每个词可能都是一个 transaction，历史太碎
- **太长（如2000ms）**：用户停下来思考，再输入，本应是两个操作，却合并成了一个
- **500ms**：经验值，大部分人打字间隔 < 500ms，思考/停顿 > 500ms

---

### 步骤3: 处理边界情况 - 撤销打断防抖 ⚠️

**问题场景**：

```
1. 用户输入 "hello"
2. 防抖定时器启动（500ms）
3. 用户在 200ms 时按 Ctrl+Z（想撤销之前的操作）
4. ⚠️ 但 300ms 后，防抖定时器到期，把 "hello" 记录了！
5. 结果：撤销的内容又被记录回来了
```

**解决方案**：撤销时清除防抖定时器

**位置**：`EditorContent.vue` 中的 `handleKeyDown`

**修改 undo 部分**：

```typescript
// 撤销：Ctrl+Z / Cmd+Z
if (isMod && event.key === 'z' && !event.shiftKey) {
  event.preventDefault()

  // ⚡️ 新增：清除防抖定时器并强制记录当前内容
  if (inputTimer !== null) {
    clearTimeout(inputTimer)
    inputTimer = null
    // 强制记录当前内容（防抖期间的输入）
    historyAPI.pushTransaction(editorState.content, '输入文本')
  }

  const previousContent = historyAPI.undo()
  if (previousContent !== null && editorRef.value) {
    editorRef.value.innerHTML = previousContent
    editorState.content = previousContent
    emit('update:modelValue', previousContent)
  }
  return
}
```

**重点**：在撤销前，如果有正在防抖的输入，先强制记录它，然后再撤销。

**修改 redo 部分**（同样的逻辑）：

```typescript
// 重做：Ctrl+Shift+Z / Cmd+Shift+Z
if (isMod && event.key.toLowerCase() === 'z' && event.shiftKey) {
  event.preventDefault()

  // ⚡️ 新增：清除防抖定时器并强制记录
  if (inputTimer !== null) {
    clearTimeout(inputTimer)
    inputTimer = null
    historyAPI.pushTransaction(editorState.content, '输入文本')
  }

  const nextContent = historyAPI.redo()
  if (nextContent !== null && editorRef.value) {
    editorRef.value.innerHTML = nextContent
    editorState.content = nextContent
    emit('update:modelValue', nextContent)
  }
  return
}
```

---

### 步骤4: 组件销毁时清理定时器 🧹

**问题**：如果用户正在输入时关闭了编辑器，定时器可能会造成内存泄漏。

**解决方案**：在组件卸载时清除定时器

**位置**：`EditorContent.vue` 的 `<script setup>`

**新增代码**：

```typescript
import { ref, reactive, watch, onUnmounted } from 'vue' // ⚡️ 导入 onUnmounted

// ... 其他代码 ...

// ⚡️ 新增：组件卸载时清理
onUnmounted(() => {
  if (inputTimer !== null) {
    clearTimeout(inputTimer)
    inputTimer = null
  }
})
```

---

## 五、完整的修改示例

### EditorContent.vue 的修改

```typescript
<script lang="ts" setup>
import { ref, reactive, watch, onUnmounted } from 'vue'  // ⚡️ 1. 导入 onUnmounted
import type { EditorState } from '../types/editor'
import { useSelection } from '../composables/useSelection'
import { useMarkdown } from '../composables/useMarkdown'
import { useHistory } from '../composables/useHistory'

// =========== Props & Emits ============
const props = defineProps<{ modelValue?: string }>()
const emit = defineEmits<{ (e: 'update:modelValue', value: string): void }>()

// =========== 响应式数据 ================
const editorRef = ref<HTMLDivElement | null>(null)
const editorState = reactive<EditorState>({ ... })

// ⚡️ 2. 新增：防抖定时器
let inputTimer: number | null = null

// ============ 监听 Props ===========
// ... 不变 ...

// ============ 监听 editorState.content ===========
// ... 不变 ...

// ============ 方法 ===========
// ⚡️ 3. 修改：输入事件处理（添加防抖）
const handleInput = () => {
  if (editorRef.value) {
    const newContent = editorRef.value.innerHTML

    // 立即更新显示
    emit('update:modelValue', newContent)
    editorState.content = newContent
    editorState.isDirty = true

    // 防抖记录历史
    if (inputTimer !== null) {
      clearTimeout(inputTimer)
    }

    inputTimer = window.setTimeout(() => {
      historyAPI.pushTransaction(newContent, '输入文本')
      inputTimer = null
    }, 500)
  }
}

// ⚡️ 4. 修改：快捷键处理（添加防抖打断）
const handleKeyDown = (event: KeyboardEvent) => {
  const isMod = event.ctrlKey || event.metaKey

  // 撤销：Ctrl+Z / Cmd+Z
  if (isMod && event.key === 'z' && !event.shiftKey) {
    event.preventDefault()

    // 清除防抖定时器并强制记录
    if (inputTimer !== null) {
      clearTimeout(inputTimer)
      inputTimer = null
      historyAPI.pushTransaction(editorState.content, '输入文本')
    }

    const previousContent = historyAPI.undo()
    if (previousContent !== null && editorRef.value) {
      editorRef.value.innerHTML = previousContent
      editorState.content = previousContent
      emit('update:modelValue', previousContent)
    }
    return
  }

  // 重做：Ctrl+Shift+Z / Cmd+Shift+Z
  if (isMod && event.key.toLowerCase() === 'z' && event.shiftKey) {
    event.preventDefault()

    // 清除防抖定时器并强制记录
    if (inputTimer !== null) {
      clearTimeout(inputTimer)
      inputTimer = null
      historyAPI.pushTransaction(editorState.content, '输入文本')
    }

    const nextContent = historyAPI.redo()
    if (nextContent !== null && editorRef.value) {
      editorRef.value.innerHTML = nextContent
      editorState.content = nextContent
      emit('update:modelValue', nextContent)
    }
    return
  }
}

// ======== 初始化 composables =========
// ... 不变 ...

// ⚡️ 5. 新增：组件卸载时清理
onUnmounted(() => {
  if (inputTimer !== null) {
    clearTimeout(inputTimer)
    inputTimer = null
  }
})

// ======= 暴露 API 给父组件 =========
// ... 不变 ...
</script>
```

---

## 六、测试方法

### 测试1: 连续输入合并

```
1. 输入 "hello" → 不要停顿
2. 等待 500ms
3. 按 Ctrl+Z → 应该删除整个 "hello" ✅
```

### 测试2: 分段输入

```
1. 输入 "hello" → 等待 600ms（让防抖记录）
2. 输入 "world"
3. 按 Ctrl+Z → 应该删除 "world"  ✅
4. 再按 Ctrl+Z → 应该删除 "hello" ✅
```

### 测试3: 输入中撤销

```
1. 输入 "test"
2. 立即按 Ctrl+Z（不等防抖）
3. 应该：
   - "test" 先被记录
   - 然后撤销 "test"
   - ✅ 结果是空的
```

### 测试4: 输入 + 格式化

```
1. 输入 "hello" → 等待 600ms
2. 选中 "hello" → 点击加粗
3. 按 Ctrl+Z → 取消加粗 ✅
4. 再按 Ctrl+Z → 删除 "hello" ✅
```

---

## 七、常见问题

### Q1: 为什么用 `window.setTimeout` 而不是 `setTimeout`？

**答**：在 TypeScript 中，`window.setTimeout` 返回 `number`，而 `setTimeout` 在 Node.js 环境返回 `NodeJS.Timeout`。使用 `window.setTimeout` 避免类型冲突。

### Q2: 可以用 Vue 的 `useDebounceFn` 吗？

**可以**，如果项目用了 VueUse：

```typescript
import { useDebounceFn } from '@vueuse/core'

const debouncedPush = useDebounceFn((content: string) => {
  historyAPI.pushTransaction(content, '输入文本')
}, 500)

const handleInput = () => {
  // ... 立即更新
  debouncedPush(newContent)
}
```

但手动实现更直观，也不依赖外部库。

### Q3: 如果用户一直在输入，永远不会记录吗？

**会**！因为：

- 用户最终会停下来（停顿 > 500ms）
- 或者按 Enter 换行（触发其他事件）
- 或者按 Ctrl+Z 撤销（强制记录）

---

## 八、编码清单 ✅

- [ ] 导入 `onUnmounted`
- [ ] 定义 `inputTimer` 变量
- [ ] 修改 `handleInput` 实现防抖
  - [ ] 立即更新显示
  - [ ] 清除旧定时器
  - [ ] 设置新定时器（500ms）
  - [ ] 定时器到期时调用 `pushTransaction`
- [ ] 修改 `handleKeyDown` 处理防抖打断
  - [ ] 撤销时清除定时器并强制记录
  - [ ] 重做时清除定时器并强制记录
- [ ] 添加 `onUnmounted` 清理定时器
- [ ] 测试所有场景

---

## 九、下一步

完成阶段4后，你的编辑器就有了**完整的撤销/重做功能**：

- ✅ 格式化操作可以撤销
- ✅ 用户输入可以撤销
- ✅ 防抖合并优化了用户体验

**阶段5** 会添加工具栏的撤销/重做按钮，让功能更完善。

---

准备好了吗？打开 `EditorContent.vue` 开始编码吧！💪
