# 登录页面基础布局 - 技术要点

> **适用阶段**：Phase 3 - 登录功能开发  
> **前置知识**：HTML/CSS 基础、Vue 3 基础  
> **学习目标**：掌握 Vue Router 路由配置、Flexbox 居中布局、CSS 变量

---

## 一、Vue Router 路由配置

### 1.1 什么是路由？

**路由** = URL 路径和页面组件的映射关系

```
用户访问 /login   →  显示 LoginView.vue 组件
用户访问 /        →  显示 HomeView.vue 组件
用户访问 /editor  →  显示 EditorView.vue 组件
```

**核心概念**：
- **路径 (path)**：URL 中的路径部分，如 `/login`
- **组件 (component)**：路径对应展示的 Vue 组件
- **路由表 (routes)**：所有路径和组件的映射数组

### 1.2 路由配置的基本结构

```typescript
// router/index.ts
import { createRouter, createWebHistory } from 'vue-router'
import HomeView from '@/views/HomeView.vue'

const router = createRouter({
  // 历史模式：URL 看起来更干净（没有 # 号）
  history: createWebHistory(import.meta.env.BASE_URL),
  
  // 路由数组：定义所有页面路由
  routes: [
    {
      path: '/',              // URL 路径
      name: 'home',           // 路由名称（可选）
      component: HomeView,    // 对应的组件
    },
    {
      path: '/login',
      name: 'login',
      component: LoginView,
    }
  ]
})

export default router
```

**配置项说明**：

| 属性          | 作用           | 示例                       |
| ----------- | ------------ | ------------------------ |
| `path`      | URL 匹配路径     | `'/login'`               |
| `name`      | 路由名称（编程式导航用） | `'login'`                |
| `component` | 路径对应的组件      | `LoginView`              |
| `meta`      | 路由元信息（如权限标记） | `{ requiresAuth: true }` |

### 1.3 懒加载路由（推荐）

**问题**：如果所有页面都打包到一起，首屏加载会很慢

**解决**：路由懒加载 - 访问时才加载对应组件

```typescript
// ❌ 普通导入：打包时全部加载
import LoginView from '@/views/LoginView.vue'

// ✅ 懒加载：访问时才加载
{
  path: '/login',
  name: 'login',
  component: () => import('@/views/LoginView.vue')
}
```

**原理**：
- `() => import()` 返回一个 Promise
- Vite/Webpack 会把懒加载组件单独打包
- 用户访问该路由时才下载对应的 JS 文件

> 💡 **记忆技巧**：`() => import()` 就像说"等需要时再去取"

### 1.4 路由器挂载到应用

```typescript
// main.ts
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'

const app = createApp(App)
app.use(router)  // 挂载路由器
app.mount('#app')
```

---

## 二、CSS Flexbox 居中布局

### 2.1 为什么需要居中？

登录页面的典型布局：**登录卡片在页面正中央**

```
┌──────────────────────────────┐
│                              │
│         ┌────────┐           │
│         │ 登录卡片│           │  ← 水平垂直居中
│         └────────┘           │
│                              │
└──────────────────────────────┘
```

### 2.2 Flexbox 居中三板斧

```css
.login-page {
  display: flex;              /* 1️⃣ 开启 Flex 布局 */
  justify-content: center;    /* 2️⃣ 水平居中（主轴） */
  align-items: center;        /* 3️⃣ 垂直居中（交叉轴） */
  min-height: 100vh;          /* 4️⃣ 占满整个视口高度 */
}
```

### 2.3 图解 Flexbox 轴线

```
         主轴 (Main Axis) - 水平方向
         justify-content: center
              ◄─────────►
        ┌─────────────────────────┐
        │                         │  ▲
        │      ┌─────────┐        │  │ 交叉轴 (Cross Axis)
        │      │  子元素  │        │  │ align-items: center
        │      └─────────┘        │  │
        │                         │  ▼
        └─────────────────────────┘
```

**核心属性对照**：

| 属性 | 作用方向 | 常用值 |
|------|----------|--------|
| `justify-content` | 主轴（默认水平） | `center`、`space-between`、`flex-start` |
| `align-items` | 交叉轴（默认垂直） | `center`、`flex-start`、`stretch` |

> 💡 **记忆技巧**：
> - justify = 对齐文字（水平）
> - align = 对齐行（垂直）

### 2.4 视口高度单位 vh

```css
min-height: 100vh;
```

- `vh` = viewport height（视口高度的 1%）
- `100vh` = 整个浏览器窗口的高度
- `min-height` 而非 `height`：允许内容超出时自动扩展

**为什么不用 100%？**
```css
/* ❌ 可能不生效，因为父元素可能没有高度 */
height: 100%;

/* ✅ 直接使用视口高度，不依赖父元素 */
min-height: 100vh;
```

---

## 三、CSS 变量（设计 Token）

### 3.1 什么是 CSS 变量？

CSS 变量是**可复用的值**，定义一次，到处使用。

```css
/* 定义变量（通常在 :root 中，表示全局） */
:root {
  --color-primary: #3B82F6;       /* 主色 */
  --color-bg-page: #F3F4F6;       /* 页面背景 */
  --spacing-lg: 24px;             /* 间距 */
  --radius-md: 8px;               /* 圆角 */
}

/* 使用变量 */
.button {
  background: var(--color-primary);
  padding: var(--spacing-lg);
  border-radius: var(--radius-md);
}
```

### 3.2 CSS 变量语法

| 语法 | 说明 | 示例 |
|------|------|------|
| `--name` | 定义变量（必须以 `--` 开头） | `--color-primary: #3B82F6;` |
| `var(--name)` | 使用变量 | `color: var(--color-primary);` |
| `var(--name, fallback)` | 使用变量，带默认值 | `color: var(--color-primary, blue);` |

### 3.3 :root 选择器

```css
:root {
  --color-primary: #3B82F6;
}
```

- `:root` 选择文档的根元素（HTML 中就是 `<html>`）
- 在 `:root` 中定义的变量是**全局变量**，任何地方都能使用
- 可以在任何选择器中定义变量，但作用域仅限于该选择器及其子元素

### 3.4 为什么使用 CSS 变量？

| 优势 | 说明 |
|------|------|
| **统一管理** | 修改一处，处处生效 |
| **主题切换** | 暗色模式只需改变量值 |
| **语义化** | `--color-primary` 比 `#3B82F6` 更易懂 |
| **可维护** | 设计变更只改一个地方 |

### 3.5 暗色主题切换原理

```css
/* 亮色主题（默认） */
:root {
  --color-bg: #FFFFFF;
  --color-text: #1F2937;
}

/* 暗色主题（通过属性选择器覆盖） */
[data-theme="dark"] {
  --color-bg: #1F2937;
  --color-text: #F9FAFB;
}

/* 使用变量的样式不需要改 */
.page {
  background: var(--color-bg);
  color: var(--color-text);
}
```

```html
<!-- 切换主题只需改 data-theme 属性 -->
<html data-theme="light">  <!-- 亮色 -->
<html data-theme="dark">   <!-- 暗色 -->
```

---

## 四、实践练习

### 练习 1：理解 Flexbox

在浏览器开发者工具中尝试修改以下属性，观察效果变化：

```css
.container {
  display: flex;
  justify-content: center;     /* 改成 flex-start、flex-end、space-between */
  align-items: center;         /* 改成 flex-start、flex-end、stretch */
}
```

### 练习 2：CSS 变量

创建一个简单的 HTML 文件，定义几个 CSS 变量并使用：

```html
<style>
  :root {
    --my-color: red;
    --my-size: 20px;
  }
  
  .box {
    background: var(--my-color);
    padding: var(--my-size);
  }
</style>

<div class="box">Hello</div>
```

---

## 五、知识点检查清单

开始编码前，请确认你理解了以下内容：

- [ ] Vue Router 的 `path` 和 `component` 属性作用
- [ ] `() => import()` 懒加载语法的含义
- [ ] Flexbox 居中的三个核心属性
- [ ] `min-height: 100vh` 的作用
- [ ] CSS 变量的定义 `--xxx` 和使用 `var(--xxx)`
- [ ] `:root` 选择器的含义

---

## 六、参考资料

- [Vue Router 官方文档](https://router.vuejs.org/zh/)
- [Flexbox 完整指南 - CSS-Tricks](https://css-tricks.com/snippets/css/a-guide-to-flexbox/)
- [CSS 变量 - MDN](https://developer.mozilla.org/zh-CN/docs/Web/CSS/Using_CSS_custom_properties)
